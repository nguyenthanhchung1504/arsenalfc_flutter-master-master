# Tuỳ chọn: sao chép thành `.env` (không commit).
# App production KHÔNG cần file này — lịch thi đấu đọc Firestore cache (TheSportsDB).
# Chỉ dùng nếu debug gọi API trực tiếp từ mobile (không khuyến nghị).

# api-football (chỉ debug, gói free không có mùa hiện tại):
# APIFOOTBALL_PROVIDER=apisports
# APIFOOTBALL_BASE_URL=https://v3.football.api-sports.io
# APIFOOTBALL_KEY=
